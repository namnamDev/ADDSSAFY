http://your-mattermost-url.com/api/v4/channels/{channel_id}/members\
채널아이디, 추가하고싶은 유저아이디로 mm채널에 초대(token은 이미 mm에들어와있는사람)
function(){
    const mmtoken:string|null = localStorage.getItem('mmtoken')
    if(typeof mmtoken === 'string'){
        axios.post(`/api/v4/channels/{channel_id}/members`,
        {
            user_id: "유저아이디"
        },
        {
            headers:{Authorization:mmtoken}
        })
    }