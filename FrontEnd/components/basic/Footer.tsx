import React, { ReactElement } from 'react'

interface Props {
    
}

function Footer({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default Footer
