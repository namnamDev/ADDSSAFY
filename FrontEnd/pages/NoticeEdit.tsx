import React, { ReactElement } from 'react'

interface Props {
    
}

function NoticeEdit({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default NoticeEdit
